import React from "react";

const NavigationTitle = ({ title }) => {
  return (
    <div className="text-center text-2xl font-bold text-gray-800">{title}</div>
  );
};

export default NavigationTitle;
